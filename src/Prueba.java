public class Prueba {
    public static void main(String[] args) {


/*
Buscar
        int [] n1 = {39, 43, 26, 37, 33, 11, 47, 9, 36, 20};
        int [] n2 = {33, 36, 27, 19, 31, 44, 37, 49, 9, 10};

        int suma = 0;
        for (int i = 0; i < n1.length; i++) {
            for (int j = 0; j < n2.length; j++) {
                if (n1[i] == n2[j]){
                    System.out.println("Es igual: " + n2[j]);
                    suma += n2[j];
                }
            }
        }

        System.out.println(suma);

 */
        /*for (int i = 0; i < n.length; i++) {
            for (int j = i + 1; j < n.length; j++) {
                if (n[i] == n[j]) {
                    // Si hay un duplicado, asigna 0 al elemento duplicado
                    n[j] = 0;
                }
            }
        }

        for (int i = 0; i < n.length; i++) {
            System.out.println(n[i]);
        }

         */

        /*
        UN SOLO ARRAY
        Random random = new Random();
        int [] arrayUno = new int[10];
        int [] arrayDos = new int[5];
        int[] arrayUnion = new int[arrayUno.length+arrayDos.length];

        System.out.println("Array uno:");
        for (int i = 0; i < arrayUno.length; i++) {
            arrayUno[i] = random.nextInt(1, 101);
            arrayUnion[i] =arrayUno[i];

            System.out.println(arrayUno[i]);
        }

        System.out.println("Array dos:");
        for (int i = 0; i < arrayDos.length; i++) {
            arrayDos[i] = random.nextInt(1, 101);
            arrayUnion[arrayUno.length+i] = arrayDos[i];
            System.out.println(arrayDos [i]);
        }


        System.out.println("Array unidos:");
        for (int i = 0; i < arrayUnion.length; i++) {
            System.out.println(arrayUnion[i]);
        }

        * */

    }
}
